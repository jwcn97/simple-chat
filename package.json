{
  "name": "simple-chat-connections-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "WebSocket + Redis pub/sub chat prototype, backed by Amazon Keyspaces (Cassandra)",
  "main": "server/gateway.js",
  "type": "module",
  "scripts": {
    "gateway": "node --env-file=server/.env server/gateway.js",
    "client": "node client/client.js"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "redis": "^4.7.0",
    "cassandra-driver": "^4.9.0"
  }
}
